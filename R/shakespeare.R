#' Corpus of Shakespeare's plays
#'
#' Full tagged text of Shakespearean plays, in XML form with stage direction,
#' scenes, and dialogue tagged.
#' @format readtext data frame with 37 plays, each with `doc_id` (giving type
#'   and title) and `text`.
#' @seealso [from_play()] to extract subsets of text from each play.
"shakespeare_corpus"
